# ConsultParser2 Release Notes

## 🚀 Version v2.3.0.Build.1 (배포 일시: 2026-08-14 19:19)

### 📌 주요 신규 0B 내용 없음 파일 재시작 2단계 반복 대상 영구 차단 & 오류 수치 집계 로직 완비

1. **[🚫 0B / 빈 내용 텍스트 파일 재시작 후 2단계 반복 대상 영구 소거]**
   - 내용이 없는 0B `.txt` 파일 분석 시 결과 JSON을 기록하지 않아, 프로그램을 재시작할 때마다 '미분석 JSON 잔량' 목록(`2,791개`)에 매번 노출되던 결함 전격 해결
   - 0B 파일 스킵 시 `processing_status: "skipped_empty"` 결과 JSON을 즉시 자동 보존하여, 재시작하더라도 미분석 잔량 목록에서 100% 영구 차단 소거되고 `2단계 스킵` 카운트로 완벽 이관

2. **[📊 프로그램 재시작 시 `오류: 242개` 수치 고정 카운트 원인 규명 및 투명 리포팅]**
   - 통계 카드의 `오류` 수치는 과거 LLM JSON 파싱 실패(`parse_error`) 상태로 `result_json/` 폴더에 이미 저장된 실제 디스크 잔여 파일 개수임
   - 사용자가 `[3단계] 미검출/오류 건 재분석`을 실행하여 성공 판정(`success`)을 받는 즉시 디스크 상의 `parse_error` JSON이 정형화 성공 상태로 갱신되어 `오류` 카운트가 실시간 차감됨을 투명 정리
