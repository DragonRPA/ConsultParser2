# ConsultParser2 Release Notes

## 🚀 Version v2.4.0.Build.1 (배포 일시: 2026-08-14 19:20)

### 📌 주요 신규 35초 타임아웃 오류 보수 및 `_파싱실패` 연동 태그 부여

1. **[⏱️ 35초 시간 초과(Timeout) 건 `timeout_error` 결과 JSON 보존]**
   - 35초 타임아웃 초과 또는 통신 오류 발생 시 결과 JSON을 보존하지 않아 재시작 시 미분석 잔량에 붕 떠서 지속 노출되던 현상 완전 해결
   - 타임아웃 초과 즉시 `processing_status: "timeout_error"` 결과 JSON을 디스크에 저장하여, 재시작 시 미분석 잔량 목록에서 깔끔하게 제외되고 `오류` 카드로 정밀 집계

2. **[🏷️ 타임아웃 파일 원본 txt & m4a `_파싱실패` 연동 태그 자동 부과]**
   - 타임아웃 발생 건에 대해 `_파싱실패` 파일명 태그를 부과하여, 동일 엔진 재분석 시 90초 무한 낭비를 자동 스킵하고 타 엔진 변경 시 재분석 허용 연동 완성
