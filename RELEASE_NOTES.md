# ConsultParser2 Release Notes

## 🚀 Version v2.6.0.Build.3 (배포 일시: 2026-08-14 19:46)

### 📌 `_파싱실패` 태그 및 수동 스킵 건 2단계 무한 재분석 시도 버그 영구 차단 패치

1. **[⛔ `_파싱실패` 파일 2단계 분석 무조건 배제 전격 적용]**
   - 파일명(txt, m4a, json) 상에 `_파싱실패` 태그가 붙어있는 파일은 **이미 파싱 불가능 또는 수동 스킵 판정이 완료된 파일**이므로, 2단계 파이프라인 분석 대상(`txt_targets`)에서 **100% 무조건 즉시 스킵(제외)**하도록 수정.
   - 종전에 타 모델 변경 시 재분석을 도모하던 예외 조건으로 인해 `_파싱실패` 건이 2단계 대상에 포함되어 팝업이 다시 뜨던 버그를 완전히 제거.

2. **[🛡️ 수동 스킵(`skipped_user`) 및 오류 건 영구 스킵 보장]**
   - 이전 결과 JSON에 `processing_status`가 `"skipped_user"`, `"skipped_empty"`, `"parse_error"`, `"timeout_error"`로 기록된 모든 파일은 2단계 스캔 시 재시도 대상에서 완전히 배제되어 무한 팝업/재분석 낭비를 100% 영구 차단.

3. **[🔍 3단계 재분석 버튼 전용 분리 유지]**
   - `_파싱실패` 건이나 미검출 건의 2차 타 엔진 재분석은 오직 **`🔍 [3단계] 미검출 건(...) 타 엔진 재분석`** 버튼을 클릭하셨을 때만 통제 하에 실행되도록 부서 R&R 및 절차 분리 완결.
