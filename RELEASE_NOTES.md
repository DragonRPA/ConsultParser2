# ConsultParser2 Release Notes

## 🚀 Version v2.6.0.Build.2 (배포 일시: 2026-08-14 19:44)

### 📌 1초 수동 스킵 즉시 전환 강화 & `_파싱실패` 파일 정형화 연동 패치

1. **[⚡ 수동 스킵 시 0초 즉시 다음 파일 전환 강화]**
   - 모달에서 **[🚫 파싱 불가 / 스킵 (0초)]** 버튼 클릭 시, 다이얼로그 즉시 소멸(`deleteLater()`) 및 메인 렌더링 큐(`QApplication.processEvents()`)를 즉시 갱신하여 0.01초의 딜레이도 없이 다음 파일 팝업이 바로 뜨도록 보완.

2. **[🏷️ 디스크 파일 `_파싱실패` 태그 즉시 부여]**
   - 사용자가 수동 스킵한 파일에 대해 결과 JSON 생성뿐만 아니라 디스크 상의 원본 `.txt` 및 `completed_audio` 폴더의 `.m4a` 파일명 뒤에도 `_파싱실패` 태그를 1:1 동기화하여 재스캔 시 영구 차단.

3. **[📊 스킵 통계 카드 실시간 카운팅 동기화]**
   - 수동 스킵 발생 시 `2단계 txt >> Json` 집계 통계 카드의 `스킵` 카운터가 실시간 1씩 즉각 올라가도록 동기화.

4. **[💡 모드 선택 유의사항 안내]**
   - `🟢 1+2단계` 실행 시에는 1단계 OpenAI Whisper STT 변환 연산(음성 1건당 10~30초)이 먼저 진행되므로, 이미 변환 완료된 대화록들을 팝업으로 빠르게 스킵하고자 하실 때는 **`🟣 2단계만 (txt ➔ JSON 분석)`** 모드로 실행하시면 0초 스킵 속도를 100% 체감하실 수 있습니다.
