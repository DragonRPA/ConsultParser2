# ConsultParser2 Release Notes

## 🚀 Version v2.7.0.Build.1 (배포 일시: 2026-08-14 19:51)

### 🏛️ [헌장 V 5.4 이행] 파싱 불가 스킵 메커니즘 전면 원칙론적 재설계 개편

1. **[🚫 스킵 클릭 시 구글드라이브 동기 파일 I/O 100% 제거]**
   - 종전: [🚫 파싱 불가 / 스킵] 클릭 시 결과 JSON 생성 + 디스크 상의 `m4a`, `txt`, `json` 파일 3개 동기 리네임 조치 집행으로 인해 구글드라이브 잠금(Lock) 대기 지연(1~3초) 발생.
   - 개편: 스킵의 본질에 맞추어 디스크 쓰기 및 리네임 조치를 **전격 소거**하고, **0.00초 인메모리 패스(Memory-Only Pass)** 방식으로 전환하여 버튼을 누르는 순간 딜레이 없이 0.00초 만에 다음 파일로 통과!

2. **[🤖 15자 미만 / 스팸 / 연결음 자동 사전 스킵 (Zero-Click Auto Filter)]**
   - 텍스트 길이가 15자 미만("노트도 1등당첨을 원하시면" 등 단문)이거나 "통화연결음", "연결되지 않았습니다", "광고" 문구가 포함된 파일은 **팝업 다이얼로그조차 띄우지 않고 2단계 진입 직후 0초 만에 100% 자동 스킵** 처리.
   - 사장님께서 의미 없는 스팸/단문 대화록에 불필요하게 팝업을 확인하고 버튼을 누르셔야 했던 노동을 원천 소거.

3. **[⚡ 진정한 0초 인메모리 파이프라인 정립]**
   - 수동 스킵 클릭 및 자동 사전 차단 건 모두 디스크/LLM 파싱 로직을 일절 수행하지 않으므로 CPU/GPU/네트워크 자원 소비 0% 달성.
