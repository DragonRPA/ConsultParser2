# ConsultParser2 Release Notes

## 🚀 Version v1.5.0.Build.1 (배포 일시: 2026-08-14 15:41)

### 📌 주요 신규 기능 및 헌장 준수 개편 사항

1. **[⚠️ JSON 파싱 실패 건 `_파싱실패` 파일명 태그 부여 & 스마트 스킵 구축]**
   - 2단계 LLM 분석 중 JSON 파싱 실패(`parse_error`) 발생 시, **원본 `.txt` 및 `.m4a` 파일명 끝에 `_파싱실패` 태그 자동 부여**
   - **동일 조건 재작업 100% 방지**: 동일한 분석 모델 구동 시 `_파싱실패` 태그 파일은 자동 스킵하여 90초 타임아웃 무한 재시도 지연을 100% 제거

2. **[🔄 다른 분석 엔진 전환 시 재작업 및 실패 태그 자동 제거 허용]**
   - 사장님이 설정 탭에서 다른 엔진(예: Ollama ➔ Google Gemini 3.7 Flash 등)으로 전환하거나 `[3단계] 타 엔진 재분석`을 집행할 경우, **`_파싱실패` 건에 대한 재분석 시도 100% 허용**
   - 타 엔진으로 재분석 성공 시, 파일명에서 `_파싱실패` 태그를 깔끔하게 지우고 **원래의 정형화 파일명으로 원복**

3. **[🛠️ 명시적 '파일명 점검 및 수정' 버튼 신설 & 프로그레스 바 연동]**
   - 사장님 지시가 있을 때만 명시적으로 txt 기준 파일명 1:1 정형화 동기화 수정 집행 (프로그레스 바 수치 및 % 실시간 시각화)
