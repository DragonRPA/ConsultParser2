# ConsultParser2 Release Notes

## 🚀 Version v1.5.1.Build.1 (배포 일시: 2026-08-14 15:42)

### 📌 주요 신규 기능 및 헌장 준수 개편 사항

1. **[⚡ LLM 타임아웃 300초 ➔ 35초 대폭 축소 & 헛생성 무한 대기 100% 방지]**
   - 기존 300초(5분)로 길게 잡혀 있던 Ollama API HTTP 타임아웃을 **35초**로 대폭 축소
   - **토큰 수 제한 (`num_predict = 768`)**: JSON 구조 파싱을 제대로 하지 못하는 로컬 모델이 90초 동안 주절주절 엉뚱한 텍스트를 무한 생성하는 현상을 **15~20초 이내로 강제 멈추고 빠르게 실패 처리**하도록 최적화

2. **[⚠️ JSON 파싱 실패 건 `_파싱실패` 파일명 태그 부여 & 스마트 스킵 구축]**
   - 2단계 LLM 분석 중 JSON 파싱 실패(`parse_error`) 발생 시, **원본 `.txt` 및 `.m4a` 파일명 끝에 `_파싱실패` 태그 자동 부여**
   - 동일 조건 재작업 시 `_파싱실패` 파일 자동 스킵으로 낭비 지연 100% 제거

3. **[🔄 다른 분석 엔진 전환 시 재작업 및 실패 태그 자동 제거 허용]**
   - 타 분석 엔진으로 전환 시 `_파싱실패` 건 재분석 허용 및 성공 시 `_파싱실패` 태그 자동 제거 원복
