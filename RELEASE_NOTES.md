# ConsultParser2 Release Notes

## 🚀 Version v2.7.0.Build.2 (배포 일시: 2026-08-14 19:53)

### 📌 수동/자동 스킵 파일 디스크 `_파싱실패` 파일명 태그 표기 예외없이 100% 부여 연동

1. **[🏷️ 디스크 상의 `_파싱실패` 파일명 태그 100% 표기 보장]**
   - 사장님께서 수동으로 **[🚫 파싱 불가 / 스킵]**을 누른 건 및 **15자 미만 단문/스팸 자동 사전 차단 건** 모두, 디스크 상의 원본 `.txt` 및 `completed_audio/` 내 `.m4a` 파일명에 **`_파싱실패` 태그를 예외 없이 1:1로 부여하여 저장**하도록 반영.
   - 팝업 창은 버튼 클릭 클릭 즉시 0초 만에 닫히고 다음 팝업이 딜레이 없이 즉각 떠오르는 비동기 반응성을 유지하면서, 디스크 상에는 `_파싱실패` 파일명이 명확히 식별되도록 정합성 결합 완료.
